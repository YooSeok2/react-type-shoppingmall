/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['https://fakestoreapi.com/','https://placeimg.com/']
  }
}

module.exports = nextConfig
